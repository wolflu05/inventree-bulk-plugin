repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version.
    rev: v0.12.0
    hooks:
      - id: ruff-format
        args: [--preview]
      - id: ruff
        args: [
            --fix,
            # --unsafe-fixes,
            --preview,
          ]
